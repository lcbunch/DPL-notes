git information

Start with git status. Shows what is new and changed in the folder.

to add everything use "git add ."

then git commit -m "", which records changes to the repository 

then git push it up! 
